# treehole-exporter

导出北大树洞为 PDF 的用户脚本。

> #7970303 这个洞让我产生了无尽的导出为瓜条 PDF 的欲望，遂 vibe 了一个。

## MVP Plan

- [x] 基于 Tampermonkey 的用户脚本，监听树洞帖子页面的 URL 变化，插入导出按钮。
- [x] API 请求
- [x] 基于 Typst 生成树洞帖子及评论的 PDF，包含气泡式对话布局（ourchat/qqnt 主题）。
- [x] 导出文件命名含帖号与时间戳，自动触发浏览器下载。
- [x] HUD 加载提示、失败报错与文件生成的基础交互。
- [ ] 解决 PDF 中文字体未嵌入的问题。
  - 方案：自托管或使用稳定 CDN 的中文字体（如 Noto Sans SC / Source Han Sans CN），在 `TypstSnippet.preloadFonts` 中填入可访问的 TTF/OTF 链接，并在 `export.typ` 中保持字体名称一致。
  - 备用：将字体文件放在可公网访问的静态存储（CDN、Pages 等），或在构建产物中以内联/本地路径分发。
- [ ] 支持树洞中的图片
- [ ] 找个良辰吉日发版宣传

> 欢迎有志之士来帮忙，PR / issue 有空的话会看的😭

## 开发

- 构建：`pnpm install && pnpm build`
- 本地调试：`pnpm dev`
